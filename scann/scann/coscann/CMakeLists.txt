# set(PROTO_SOURCES
# v2_restricts.proto
# )

# protobuf_generate_cpp(PROTO_SRCS PROTO_HDRS ${PROTO_SOURCES})

# add_library(coscann_v2_restricts_cc_proto SHARED ${PROTO_SRCS} ${PROTO_HDRS})
# target_link_libraries(coscann_v2_restricts_cc_proto ${PROTOBUF_LIBRARIES})